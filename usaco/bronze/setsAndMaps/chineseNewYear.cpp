#include <algorithm>
#include <iostream>
#include <cmath>
#include <vector>
#include <queue>
#include <cstring>
#include <unordered_map>
#include <map>
#include <set>

#define all(x) (x).begin(), (x).end()

typedef long long ll;
using namespace std;

void solve(){
    map<string, int> calendar = {{"Ox", 0}, {"Tiger", 1}, {"Rabbit", 2}, {"Dragon", 3},
    {"Snake", 4}, {"Horse", 5}, {"Goat", 6}, {"Monkey", 7}, {"Rooster", 8},
    {"Dog", 9}, {"Pig", 10}, {"Rat", 11}};

    int n;
    cin >> n;
    map<string, int> birth;
    birth["Bessie"] = 0

    for(int i = 0; i < n; i++){
        string firstName, trash, when, year, secondName;
        cin >> firstName >> trash >> trash >> when >> year >> trash >> trash >> secondName;

        if(when == "previous"){
            if()
        }
        else{

        }
    }
}

int main(){
    /* freopen("whereami.in", "r", stdin);
	freopen("whereami.out", "w", stdout); */
    solve();
} 